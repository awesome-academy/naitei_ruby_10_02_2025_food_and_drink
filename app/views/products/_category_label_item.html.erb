<%= link_to category.name, products_path(request.query_parameters.except(:page).merge(category_id: category.id)),
  class: "category-pill px-3 py-1 rounded-full text-sm #{params[:category_id].to_i == category.id ? "bg-blue-600 text-white" : "bg-gray-100 text-gray-700 hover:bg-gray-200"}" %>
